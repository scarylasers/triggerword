# Optional - only for the "advanced install" (local Whisper transcription and
# the WebSocket server behind global hotkeys). The quickstart and the Windows
# installer need NONE of this: they run on the standard library alone.
#
# Floors, not pins. The old hard pins had drifted years out of date from what
# actually runs, which is how a pile of stale advisories accumulated.

numpy>=2.0
torch>=2.6.0            # 2.6.0 fixes the torch.load weights_only RCE
openai-whisper>=20250625
fastapi>=0.115.0
uvicorn>=0.30.6
websockets>=10.4        # uvicorn.run(ws="websockets") in local_server.py

# Windows-only, for the global hotkey router (triggerword_router_improved.py)
keyboard>=0.13.5; sys_platform == "win32"
pywinauto>=0.6.8; sys_platform == "win32"
pywin32>=306; sys_platform == "win32"

# Optional, for the desktop splash animation (splash.py)
pillow>=10.0
